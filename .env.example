# Hedera MultiSig Configuration
# Copy this file to .env and fill in your values
# NEVER commit your .env file to version control!

# ============================================================================
# HEDERA OPERATOR CONFIGURATION
# ============================================================================

# Account ID that pays transaction fees (required)
# Format: 0.0.XXXXX
OPERATOR_ID=0.0.12345

# Private key for the operator account (optional for multisig workflows)
# Only required for:
#   - Single-signature transactions
#   - When operator account differs from multisig account
# For multisig, signatures are collected via the signing session instead
# Format: Hex string (302e...) or DER format
OPERATOR_KEY=

# Hedera network (default: TEST)
# Options: TEST, MAIN, PREVIEW, LOCAL
#   TEST     - Hedera Testnet (development)
#   MAIN     - Hedera Mainnet (production)
#   PREVIEW  - Hedera Previewnet (early features)
#   LOCAL    - Local Hedera node (solo/docker)
ENVIRONMENT=TEST

# ============================================================================
# SESSION CONFIGURATION (for networked workflows)
# ============================================================================

# WebSocket server URL for networked signing sessions
# Set these to auto-connect without prompts
SESSION_SERVER=ws://localhost:3000

# Session ID (from coordinator)
SESSION_ID=

# Session PIN (from coordinator)
SESSION_PIN=

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Mirror node URLs (defaults are provided if not set)
# MIRROR_NODE_TESTNET=https://testnet.mirrornode.hedera.com
# MIRROR_NODE_MAINNET=https://mainnet-public.mirrornode.hedera.com

# ============================================================================
# USAGE EXAMPLES
# ============================================================================

# Single-sig transaction (requires OPERATOR_KEY):
#   node examples/transfer-hbar.js --receiver 0.0.456 --amount 10

# Multisig transaction (OPERATOR_KEY optional):
#   node examples/transfer-hbar.js --receiver 0.0.456 --amount 10 --multisig \
#     --workflow networked --server ws://localhost:3000 --session abc123 --pin 123456

# Or set SESSION_* vars above and run:
#   node examples/transfer-hbar.js --receiver 0.0.456 --amount 10 --multisig --workflow networked

# Start a signing session server:
#   npm run multisig-server -- -t 2 -k "pubkey1,pubkey2,pubkey3"

# Connect as a participant:
#   npm run multisig-client -- --url ws://localhost:3000 --session abc123 --pin 123456
